// Interrupteurs de fonctionnalités, à basculer manuellement le temps de
// résoudre un point bloquant sans supprimer le code correspondant.

// Comparateur d'insigne : suspendu le temps de clarifier les droits
// d'usage des photos de décals de référence (sourcées en ligne, non
// détenues par l'app). Repasser à true une fois le point réglé.
export const COMPARATOR_ENABLED = false;
